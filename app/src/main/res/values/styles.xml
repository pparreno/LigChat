<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppText" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/text_gray</item>
        <item name="android:letterSpacing">0.1</item>
    </style>

    <style name="AppTextError" parent="AppText">
        <item name="android:textColor">@color/error_red</item>
        <item name="android:textStyle">bold</item>
    </style>


    <style name="AppButton" parent="@android:style/Widget.Button">
        <item name="android:textColor">#FFFFFFFF</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">20sp</item>
        <item name="android:letterSpacing">0.1</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="AppField" parent="Widget.AppCompat.EditText">
        <item name="android:layout_marginStart">@dimen/side_margin</item>
        <item name="android:layout_marginEnd">@dimen/side_margin</item>
        <item name="android:background">@drawable/bg_rounded_field</item>
        <item name="android:textColor">@color/text_gray</item>
        <item name="android:layout_height">45dp</item>
        <item name="android:textSize">20sp</item>
    </style>

</resources>